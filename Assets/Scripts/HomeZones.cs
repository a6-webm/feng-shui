using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HomeZones : MonoBehaviour
{
    [SerializeField]
    List<List<GameObject>> zonesList;

    // Start is called before the first frame update
    void Start()
    {
        foreach (List<GameObject> path in zonesList) {
            
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
